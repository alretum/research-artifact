spring:
  ai:
    openai:
      chat:
        base-url: ${LOGOS_BASE_URL:https://logos.aet.cit.tum.de/v1}
        api-key: ${LOGOS_API_KEY}

mcq:
  generation:
    model: ${LOGOS_MODEL}
  filter:
    model: ${LOGOS_MODEL}
